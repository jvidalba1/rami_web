upstream unicorn {
  server unix:/tmp/unicorn.projectname.sock fail_timeout=0;
}

server {
  listen 80 default_server deferred;
  # server_name example.com;
  root /home/username/apps/projectname/current/public;

  location ^~ /assets/ {
    gzip_static on;
    expires max;
    add_header Cache-Control public;
  }

  try_files $uri/index.html $uri @unicorn;
  location @unicorn {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $http_host;
    proxy_redirect off;
    proxy_pass http://unicorn;
  }

  error_page 500 502 503 504 /500.html;
  client_max_body_size 20M;
  keepalive_timeout 10;
}

#upstream puma {
#  server unix:///var/www/rami_web/shared/tmp/sockets/rami_web-puma.sock;
#}
#
#server {
#  listen 80 default_server deferred;
# server_name example.com;
#
#  root /var/www/rami_web/current/public;
#  access_log /var/www/rami_web/current/log/nginx.access.log;
#  error_log /var/www/rami_web/current/log/nginx.error.log info;
#
#  location ^~ /assets/ {
#    gzip_static on;
#    expires max;
#    add_header Cache-Control public;
#  }
#
#  try_files $uri/index.html $uri @puma;
#  location @puma {
#    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#    proxy_set_header Host $http_host;
#    proxy_redirect off;
#
#    proxy_pass http://puma;
#  }

#  error_page 500 502 503 504 /500.html;
#  client_max_body_size 10M;
#  keepalive_timeout 10;
#}
#upstream app {
#  least_conn;
#	server web:3000 max_fails=3 fail_timeout=60 weight=1;
#}
#server {
#	listen 82 default_server;
#	location / {
#		proxy_pass http://app;
#		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#		proxy_set_header Host $host;
#		proxy_set_header X-Real-IP $remote_addr;
#	}
#}
